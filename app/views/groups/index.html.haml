%h3 Your Liftoff Groups

%table.table-condensed.table-bordered.table-striped
  %tr
    %th Name
    %th
  - @groups.each do |group|
    %tr
      %td= link_to group.name, edit_group_path(group)
      %td= "#{group.num_members} members"

%br
= link_to 'Add Group', new_group_path
